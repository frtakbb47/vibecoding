services:
  - type: web
    name: pomodoro-dual
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: HOST_TOKEN_SECRET
        generateValue: true
